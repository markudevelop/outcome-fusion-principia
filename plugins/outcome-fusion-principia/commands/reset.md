Reset Outcome Fusion state for the active session only after user approval.

Read `.ai/outcome_fusion/current_session.txt`, then archive or remove the matching folder under `.ai/outcome_fusion/sessions/<session>/`.

Do not delete `.ai/outcome_fusion/memory.md` unless the user explicitly asks to delete project memory.
Do not delete other session folders unless the user explicitly asks to reset all Outcome Fusion sessions for the repo.
