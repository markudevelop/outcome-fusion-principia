Run Outcome Fusion Principia on the current task.

Read `.ai/outcome_fusion/current_session.txt`, then use the matching session folder under `.ai/outcome_fusion/sessions/<session>/`.

Read that session's `mission.md`, `proof.md`, `review.md`, `closure.md`, and `tool_log.md` if present. Also read project memory at `.ai/outcome_fusion/memory.md` if present.

Continue using first principles, simplification, scientific verification, and release readiness.

Do not ask small questions. Make reversible assumptions and continue.
Do not say impossible without proof or a specific blocker.
Do not add complexity before removing what is not needed.
Update the session proof ledger, not a global proof file.
